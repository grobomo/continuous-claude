# TODO — [Project Name]

## Code Tasks

Pick these in continuous-claude iterations. Each should be a single, small
code change that doesn't require live infrastructure.

- [ ] Example: Fix variable reference in script.sh line 46
- [ ] Example: Add input validation to deploy function
- [ ] Example: Refactor common.sh to prefer `az` over `az.cmd` on Git Bash

## Infra Tasks (manual — do not pick these)

These require live cloud resources, long-running commands, or manual
verification. Run these outside of continuous-claude.

- [ ] Example: Deploy fresh E2E environment and run smoke test
- [ ] Example: Verify PDF report renders correctly with live data
- [ ] Example: Run terraform apply and check all VMs come up

## Notes for Next Iteration

<!-- continuous-claude updates this section automatically via SHARED_TASK_NOTES.md -->
